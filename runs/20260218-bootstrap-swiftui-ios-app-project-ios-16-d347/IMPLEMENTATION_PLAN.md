## Summary
Bootstrap an iOS 16+ SwiftUI app in `AlexLongmuir/regretless-3-ios` with: an Xcode project scaffold, secrets-safe configuration via `.xcconfig` + Info.plist substitution, a minimal `URLSession` networking stub with `os.Logger` logging, a placeholder UI that exercises the network call and shows status states, GitHub Actions CI build, and setup docs.

## Steps
1. **Create Xcode project sc

[REDACTED]
