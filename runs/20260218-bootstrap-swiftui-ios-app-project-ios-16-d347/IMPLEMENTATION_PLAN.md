## Summary
Bootstrap a native iOS 16+ SwiftUI app in `AlexLongmuir/regretless-3-ios` with: an Xcode project scaffold, secrets-safe configuration via `.xcconfig` + Info.plist substitution exposed through `AppConfig`, a minimal `URLSession` networking stub with `os.Logger` logging, a placeholder UI that can trigger a test request and show status states (idle/loading/success/error/offline), plus GitH

[REDACTED]
