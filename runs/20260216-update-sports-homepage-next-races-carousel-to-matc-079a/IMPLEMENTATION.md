## Work packages

- 1. **Carousel replica (base UI + mobile gating)** — Rebuild the Next Races carousel markup/layout/styles to match the Spreadex mobile homepage and ensure it only applies on mobile.
- 2. **States + interactions (loading/empty/error/offline/retry)** — Implement skeleton loading and all empty/error/offline states with correct strings and retry behavior matching the reference.
- 3.

[REDACTED]
