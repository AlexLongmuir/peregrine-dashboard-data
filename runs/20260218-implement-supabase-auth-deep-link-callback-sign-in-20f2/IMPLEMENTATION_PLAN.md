## Summary
Implement full Supabase-based auth for iOS (SwiftUI): add Supabase Swift SDK, initialize client from `Info.plist` config, persist/restore session on launch, handle auth callback deep links via `regretless://auth-callback`, provide native Sign in with Apple that yields a Supabase session, and switch between a login screen and an authenticated placeholder screen with sign-out. Expose a si

[REDACTED]
