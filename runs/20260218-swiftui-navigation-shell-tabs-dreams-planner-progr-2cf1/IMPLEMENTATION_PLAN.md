## Summary
Implement a SwiftUI navigation shell with a 4-tab `TabView` (Dreams, Planner, Progress, Account). Each tab owns an independent `NavigationStack` backed by its own `NavigationPath` to preserve state across tab switches. Add placeholder root screens and a Planner “Today Details” second-level screen. Add centralized deep link handling via `onOpenURL` to select tabs and (for `regretless://p

[REDACTED]
