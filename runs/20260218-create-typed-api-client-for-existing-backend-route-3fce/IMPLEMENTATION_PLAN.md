## Summary
Implement a reusable, typed networking layer for the iOS app that targets the existing Next.js backend, automatically injects the current Supabase access token, provides Codable core models, exposes typed route wrappers (Dreams CRUD + list endpoints for others), adds unit tests (request building/decoding/error handling), and minimally wires `listDreams()` into an existing Dreams list VM

[REDACTED]
